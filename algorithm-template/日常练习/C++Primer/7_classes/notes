7.1 定义抽象数据类型
7.2 访问控制和封装
7.3 额外类特性
7.4 类作用范围
7.5 构造器再访问
7.6 静态类成员

类背后的基本观点是数据抽象和封装
数据抽象是一个编程技巧,依托于接口和实现的分离
类接口由类用户可执行的操作组成
实现包含类的数据成员,组成接口的函数体,以及需要定义类的函数(不为通常使用)
封装迫使类的接口和实现分离.被封装类隐藏它的实现,类的用户可以使用接口但是不能访问其实现
使用数据抽象和封装的类定义一个抽象数据类型.

12.1 动态内存和智能指针
12.2 动态数组
12.3 使用库:文字查询程序

程序使用静态或者栈内存,静态内存用于局部静态对象,针对类静态成员以及在函数外定义的变量
栈内存用于在函数体内定义的非静态对象
分配在静态或者栈内存中的对象是被编译器自动创建和销毁
栈对象只有当其被定义的块在执行时才存在
静态对象在使用前被分配,当程序结束后被销毁
除了静态或者栈内存,每个程序也有一个它可以用的内存池.这个内存被描述为自由存储区或堆
程序对于他们动态分配的对象(程序运行时分配的对象)使用堆
程序控制动态对象的生涯,代码必须明确地摧毁这样的对象当它们不再需要时